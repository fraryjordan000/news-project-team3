// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBcZqIBnx9AoECfDc_1H0FY2F0ds00vFPs",
    authDomain: "news-project-team3.firebaseapp.com",
    databaseURL: "https://news-project-team3.firebaseio.com",
    projectId: "news-project-team3",
    storageBucket: "news-project-team3.appspot.com",
    messagingSenderId: "1059475983054"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
